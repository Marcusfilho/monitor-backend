--- a/public/app_installations_v1.html
+++ b/public/app_installations_v1.html
@@ -19,7 +19,7 @@
       border-bottom:1px solid var(--line);padding:14px 16px;display:flex;align-items:center;justify-content:space-between;gap:10px}
     header .title{display:flex;flex-direction:column;gap:2px}
     header h1{margin:0;font-size:14px;letter-spacing:.2px}
-    header .sub{font-size:12px;color:var(--muted)}
+    header .sub{font-size:12px;color:var(--muted);display:flex;align-items:center;gap:8px}
 
     .wrap{max-width:980px;margin:0 auto;padding:14px 12px 96px}
 
@@ -98,13 +98,19 @@
       .wrap{padding:12px 10px 98px}
       .chip .msg{max-width:160px}
     }
-  </style>
+  
+
+@keyframes pulse{0%{transform:scale(1);opacity:.55}50%{transform:scale(1.6);opacity:1}100%{transform:scale(1);opacity:.55}}
+@keyframes spin{to{transform:rotate(360deg)}}
+.dot.pulse{animation:pulse 1.1s ease-in-out infinite}
+.spin{display:inline-block;animation:spin 1s linear infinite}
+</style>
 </head>
 <body>
 <header>
   <div class="title">
     <h1>APP V1 — Instalações <span class="pill" style="margin-left:8px">v1.5</span></h1>
-    <div class="sub" id="subline">Pronto</div>
+    <div class="sub" id="subline"><span class="dot" id="liveDot"></span><span id="subText">Pronto</span></div>
   </div>
   <div style="display:flex;gap:10px;align-items:center">
     <div class="kv" id="topKv"></div>
@@ -247,16 +253,32 @@
   const $ = (id) => document.getElementById(id);
 
   // ====== state ======
-  const state = {
-    inst: null,
-    lastSnapshot: null,
-    pollTimer: null,
-    pollMs: 5000,
-    debug: false,
-  };
+const state = {
+  inst: null,
+  lastSnapshot: null,
+  pollTimer: null,
+  pollMs: 5000,
+  debug: false,
+  isLoading: false,
+  bestSnapshot: null,
+  bestSnapshotScore: 0,
+  bestSnapshotAt: 0,
+};
 
   // ====== ui helpers ======
-  function setSubline(msg){ $("subline").textContent = msg || ""; }
+
+function setLive(kind, pulse){
+  const d = $("liveDot");
+  if (!d) return;
+  d.className = "dot" + (kind ? (" " + kind) : "");
+  if (pulse) d.classList.add("pulse");
+}
+function setSubline(msg, kind="", pulse=false){
+  const t = $("subText") || $("subline");
+  if (t) t.textContent = msg || "";
+  setLive(kind, pulse);
+}
+
 
   function pill(text, kind){
     const s = document.createElement("span");
@@ -561,21 +583,93 @@
   }
 
   function extractSnapshot(inst){
-    const raw =
-      inst?.can_snapshot_latest ||
-      inst?.canSnapshotLatest ||
-      inst?.can_snapshot_best ||
-      inst?.canSnapshotBest ||
-      inst?.can_snapshot ||
-      inst?.canSnapshot ||
-      inst?.can?.snapshot_latest ||
-      inst?.can?.snapshot ||
-      (Array.isArray(inst?.can?.snapshots) ? inst.can.snapshots[inst.can.snapshots.length-1] : null) ||
-      null;
-
-    const snap = unwrapSnapshot(raw);
-    return snap && typeof snap === "object" ? snap : null;
-  }
+  // IMPORTANTE:
+  // - can_snapshot = "melhor snapshot" (persistido apenas quando tem dados)
+  // - can_snapshot_latest = último ciclo (pode vir parcial e não deve apagar UI)
+  const raw =
+    inst?.can_snapshot ||
+    inst?.canSnapshot ||
+    inst?.can_snapshot_best ||
+    inst?.canSnapshotBest ||
+    inst?.can?.snapshot ||
+    inst?.can?.snapshot_best ||
+    inst?.can_snapshot_latest ||
+    inst?.canSnapshotLatest ||
+    inst?.can?.snapshot_latest ||
+    (Array.isArray(inst?.can?.snapshots) ? inst.can.snapshots[0] : null) ||
+    null;
+
+  const snap = unwrapSnapshot(raw);
+  return snap && typeof snap === "object" ? snap : null;
+}
+
+
+
+function snapshotCounts(snapshot){
+  const c = (snapshot && snapshot.counts && typeof snapshot.counts === "object") ? snapshot.counts : {};
+  const params = Array.isArray(snapshot?.parameters) ? snapshot.parameters : (Array.isArray(snapshot?.params) ? snapshot.params : []);
+  const ms = Array.isArray(snapshot?.moduleState) ? snapshot.moduleState :
+             (Array.isArray(snapshot?.module_state) ? snapshot.module_state :
+              (Array.isArray(snapshot?.moduleStates) ? snapshot.moduleStates : []));
+  const paramsTotal = Number(c.params_total ?? c.parameters_total ?? params.length ?? 0) || 0;
+  const paramsWith = Number(c.params_with_value ?? c.parameters_with_value ?? 0) || 0;
+  const msKey = Number(c.module_state_key ?? c.moduleStateKey ?? 0) || 0;
+  const msTotal = Number(c.module_state_total ?? c.moduleStateTotal ?? ms.length ?? 0) || 0;
+  return { paramsTotal, paramsWith, msKey, msTotal };
+}
+
+function snapshotScore(snapshot){
+  if (!snapshot || typeof snapshot !== "object") return -1;
+  const { paramsTotal, paramsWith, msKey, msTotal } = snapshotCounts(snapshot);
+  const header = snapshot?.header || {};
+  const hasHdr = !!(header && (header.communication || header.server_time || header.license_nmbr || header.license_number || header.inner_id || header.serial || header.client || header.model || header.manufacturer));
+  // score forte para manter snapshot "bom" (evita flicker quando vier parcial)
+  return (hasHdr ? 100000 : 0) + (msKey * 5000) + (paramsWith * 50) + (paramsTotal) + (msTotal * 10);
+}
+
+function pickStableSnapshot(inst){
+  const best = extractSnapshot(inst);
+  const latest = unwrapSnapshot(inst?.can_snapshot_latest || inst?.canSnapshotLatest || inst?.can?.snapshot_latest || null);
+  const candidates = [best, latest].filter(Boolean);
+
+  // mantém o melhor na tela (não apaga por snapshot parcial)
+  let chosen = candidates.length ? candidates[0] : null;
+  let chosenScore = chosen ? snapshotScore(chosen) : -1;
+
+  for (const s of candidates.slice(1)){
+    const sc = snapshotScore(s);
+    if (sc > chosenScore) { chosen = s; chosenScore = sc; }
+  }
+
+  // se vier um snapshot muito fraco, não derruba o último válido
+  if (state.bestSnapshot){
+    const prevScore = state.bestSnapshotScore || snapshotScore(state.bestSnapshot);
+    if (chosenScore < 1 && prevScore > 0) {
+      chosen = state.bestSnapshot;
+      chosenScore = prevScore;
+    } else if (chosen && chosenScore >= prevScore) {
+      state.bestSnapshot = chosen;
+      state.bestSnapshotScore = chosenScore;
+      state.bestSnapshotAt = Date.now();
+    }
+  } else if (chosen && chosenScore > 0) {
+    state.bestSnapshot = chosen;
+    state.bestSnapshotScore = chosenScore;
+    state.bestSnapshotAt = Date.now();
+  }
+
+  return chosen || state.bestSnapshot || null;
+}
+
+
+function canReadyByData(snapshot){
+  const { paramsWith, msKey } = snapshotCounts(snapshot || {});
+  // regra simples e auditável (ajuste fino depois):
+  // - >= 3 módulos chave presentes
+  // - >= 6 parâmetros com valor (raw_value/value)
+  return (msKey >= 3) && (paramsWith >= 6);
+}
+
 
   function parsePacketAge(snapshot){
     const header = snapshot?.header || snapshot?.meta || {};
@@ -639,49 +733,70 @@
     return /^(1|true|yes|on)$/i.test(String(v || "").trim());
   }
 
-  function renderModuleState(snapshot){
-    const ms = snapshot?.module_state || snapshot?.moduleState || snapshot?.module_states || snapshot?.moduleStates || [];
-    const arr = Array.isArray(ms) ? ms : [];
-
-    const byId = new Map();
-    for (const it of arr){
-      const id = normMsId(it);
-      if (id != null && !byId.has(id)) byId.set(id, it);
-    }
-
-    const host = $("msChips");
-    host.innerHTML = "";
-
-    for (const w of MS_WANT){
-      const it = byId.get(w.id) || null;
-      const active = truthy(it?.active);
-      const ok = truthy(it?.ok);
-      const wasOk = truthy(it?.was_ok ?? it?.wasOk);
-      const msg = norm(it?.message || it?.msg || "");
-
-      let kind = "bad";
-      if (it && active && ok && wasOk) kind = "good";
-      else if (!it) kind = "warn";
-
-      const chip = document.createElement("div");
-      chip.className = "chip";
-      const d = document.createElement("span");
-      d.className = "dot " + kind;
-      const n = document.createElement("span");
-      n.className = "name";
-      n.textContent = w.key;
-      const m = document.createElement("span");
-      m.className = "msg";
-      if (!it) m.textContent = "sem dados";
-      else if (msg) m.textContent = msg;
-      else m.textContent = (kind === "good") ? "OK" : "falha";
-
-      chip.appendChild(d);
-      chip.appendChild(n);
-      chip.appendChild(m);
-      host.appendChild(chip);
-    }
-  }
+  function normMsKey(s){
+  return String(s || "").toUpperCase().replace(/[^A-Z0-9]/g, "");
+}
+
+function msKeyFromRow(it){
+  const s = normMsKey(it?.key || it?.sub || it?.sub_module_name || it?.subModuleName || it?.module_name || it?.moduleName || it?.name);
+  if (!s) return null;
+  if (s.includes("CAN0")) return "CAN0";
+  if (s.includes("CAN1")) return "CAN1";
+  if (s.includes("J1708")) return "J1708";
+  if (s.includes("KEYPADDALLAS") || s.includes("DALLAS") || s.includes("IBUTTON")) return "KEYPAD_DALLAS";
+  if (s.includes("KEYPADRAMZOR") || s.includes("RAMZOR")) return "KEYPAD_RAMZOR";
+  return null;
+}
+
+function renderModuleState(snapshot){
+  const ms = snapshot?.module_state || snapshot?.moduleState || snapshot?.module_states || snapshot?.moduleStates || snapshot?.moduleState || [];
+  const arr = Array.isArray(ms) ? ms : [];
+
+  const byId = new Map();
+  const byKey = new Map();
+  for (const it of arr){
+    const id = normMsId(it);
+    if (id != null && !byId.has(id)) byId.set(id, it);
+
+    const k = msKeyFromRow(it);
+    if (k && !byKey.has(k)) byKey.set(k, it);
+  }
+
+  const host = $("msChips");
+  host.innerHTML = "";
+
+  for (const w of MS_WANT){
+    const it = byKey.get(w.key) || byId.get(w.id) || null;
+
+    const active = truthy(it?.active);
+    const ok = truthy(it?.ok);
+    const wasOk = truthy(it?.was_ok ?? it?.wasOk);
+    const msg = norm(it?.message || it?.msg || it?.error_descr || it?.errorDescr || "");
+
+    let kind = "bad";
+    if (it && active && ok && wasOk) kind = "good";
+    else if (!it) kind = "";
+
+    const chip = document.createElement("div");
+    chip.className = "chip";
+    const d = document.createElement("span");
+    d.className = "dot " + (kind || "");
+    const n = document.createElement("span");
+    n.className = "name";
+    n.textContent = w.key;
+    const m = document.createElement("span");
+    m.className = "msg";
+    if (!it) m.textContent = "—";
+    else if (msg) m.textContent = msg;
+    else m.textContent = (kind === "good") ? "OK" : "falha";
+
+    chip.appendChild(d);
+    chip.appendChild(n);
+    chip.appendChild(m);
+    host.appendChild(chip);
+  }
+}
+
 
   function hasValue(v){
     if (v === 0) return true;
@@ -691,10 +806,48 @@
     return s !== "" && s.toLowerCase() !== "null" && s.toLowerCase() !== "nan";
   }
 
-  function pickKeyParams(snapshot, maxN){
-    const params = snapshot?.parameters || snapshot?.params || [];
-    const arr = Array.isArray(params) ? params : [];
-
+  const KEY_PARAM_RULES = [
+  { key: "speed",       label: "Speed",           re: /(vehicle\s*speed|\bspeed\b|veloc)/i },
+  { key: "rpm",         label: "RPM",             re: /(engine\s*speed|\brpm\b)/i },
+  { key: "fuel_level",  label: "Fuel level",      re: /(fuel\s*level|fuel\s*percent|nivel\s*comb|combust)/i },
+  { key: "fuel_used",   label: "Fuel used",       re: /(fuel\s*used|consumption|consumo|liter|l\/100|km\/?l)/i },
+  { key: "coolant",     label: "Coolant temp",    re: /(coolant|engine\s*temp|temp.*agua|temper)/i },
+  { key: "oil_press",   label: "Oil pressure",    re: /(oil\s*press|press.*oleo|engine\s*oil)/i },
+  { key: "brake_press", label: "Brake pressure",  re: /(brake\s*press|air\s*press|freio\s*press)/i },
+  { key: "battery",     label: "Battery voltage", re: /(battery\s*voltage|batt|tensao|voltag)/i },
+  { key: "mileage",     label: "Mileage / Odo",   re: /(gps\s*odo|odo|odomet|mileage|total\s*distance|km)/i },
+  { key: "hours",       label: "Engine hours",    re: /(engine\s*hours|\bhours\b|horimetro|hora)/i },
+];
+
+function pickKeyParams(snapshot, maxN){
+  const params = snapshot?.parameters || snapshot?.params || [];
+  const arr = Array.isArray(params) ? params : [];
+
+  const picked = [];
+  const used = new Set();
+
+  // 1) Regras (fuel/press/temp/etc)
+  for (const rule of KEY_PARAM_RULES){
+    for (const p of arr){
+      const name = String(p?.name || p?.param_name || p?.paramName || "");
+      if (!name) continue;
+      if (!rule.re.test(name)) continue;
+
+      const val = hasValue(p?.value) ? p.value : (hasValue(p?.raw_value) ? p.raw_value : null);
+      if (!hasValue(val)) continue;
+
+      const k = String(rule.key) + "|" + name;
+      if (used.has(k)) continue;
+      used.add(k);
+
+      picked.push({ name, val });
+      break;
+    }
+    if (picked.length >= maxN) break;
+  }
+
+  // 2) Complemento: ranking geral (quando faltou algo)
+  if (picked.length < maxN){
     const scored = [];
     for (const p of arr){
       const name = String(p?.name || p?.param_name || p?.paramName || "");
@@ -704,40 +857,29 @@
       const n = name.toLowerCase();
       let score = 0;
       if (/(speed|veloc)/.test(n)) score += 50;
-      if (/(rpm)/.test(n)) score += 45;
+      if (/(rpm|engine\s*speed)/.test(n)) score += 45;
       if (/(mile|odo|odomet|km)/.test(n)) score += 35;
       if (/(fuel|comb|diesel|gas)/.test(n)) score += 30;
-      if (/(hour|hora)/.test(n)) score += 25;
-      if (/(temp|temper)/.test(n)) score += 20;
-      if (/(ignit|key)/.test(n)) score += 15;
-      scored.push({ p, name, val, score });
+      if (/(press)/.test(n)) score += 28;
+      if (/(temp|temper|coolant)/.test(n)) score += 25;
+      if (/(hour|hora)/.test(n)) score += 20;
+      scored.push({ name, val, score });
     }
 
     scored.sort((a,b)=> (b.score - a.score) || String(a.name).localeCompare(String(b.name)));
 
-    const out = [];
-    const seen = new Set();
     for (const it of scored){
-      const k = it.name || String(it.p?.id || "");
-      if (!k || seen.has(k)) continue;
-      seen.add(k);
-      out.push({ name: it.name || ("id " + String(it.p?.id || "?")), val: it.val });
-      if (out.length >= maxN) break;
-    }
-
-    // fallback: pega os primeiros com valor (se nada ranqueado)
-    if (!out.length){
-      for (const p of arr){
-        const name = String(p?.name || p?.param_name || p?.paramName || "");
-        const val = hasValue(p?.value) ? p.value : (hasValue(p?.raw_value) ? p.raw_value : null);
-        if (!hasValue(val)) continue;
-        out.push({ name: name || ("id " + String(p?.id || "?")), val });
-        if (out.length >= maxN) break;
-      }
-    }
-
-    return out;
-  }
+      const k = it.name;
+      if (!k || used.has(k)) continue;
+      used.add(k);
+      picked.push({ name: it.name, val: it.val });
+      if (picked.length >= maxN) break;
+    }
+  }
+
+  return picked.slice(0, maxN);
+}
+
 
   function renderKeyParams(snapshot){
     const host = $("keyParams");
@@ -757,6 +899,25 @@
     top.className = "li";
     top.innerHTML = `<div class="k">Parameters</div><div class="v">${withVal}/${total}</div>`;
     host.appendChild(top);
+
+
+// Header "principais" (quando disponível)
+const hdr = (snapshot && snapshot.header && typeof snapshot.header === "object") ? snapshot.header : {};
+const hdrRows = [
+  ["Ignition", hdr.ignition ?? (hdr.raw && hdr.raw.ignition)],
+  ["Speed", hdr.speed],
+  ["Fuel", hdr.fuel],
+  ["Mileage", hdr.mileage],
+  ["Engine hours", hdr.engine_hours],
+];
+
+for (const [kName, vVal] of hdrRows){
+  if (!hasValue(vVal)) continue;
+  const li = document.createElement("div");
+  li.className = "li";
+  li.innerHTML = `<div class="k">${kName}</div><div class="v">${String(vVal)}</div>`;
+  host.appendChild(li);
+}
 
     const picked = pickKeyParams(snapshot, 8);
     for (const it of picked){
@@ -800,35 +961,68 @@
   }
 
   function renderStepper(status){
-    const steps = [
-      { b:"HTML5", s:"cadastro" },
-      { b:"SB", s:"atualizando" },
-      { b:"CAN", s:"coletando" },
-      { b:"Aprovar", s:"técnico" },
-      { b:"GS", s:"enviado" },
-      { b:"Fim", s:"ok" },
-    ];
-
-    const idx = statusToStepIndex(status);
-    const host = $("stepper");
-    host.innerHTML = "";
-
-    for (let i=0;i<steps.length;i++){
-      const it = steps[i];
-      const el = document.createElement("div");
-      el.className = "step";
-      const d = document.createElement("span");
-      d.className = "dot " + (i < idx ? "good" : (i === idx ? "warn" : ""));
-      const b = document.createElement("b");
-      b.textContent = it.b;
-      const s = document.createElement("span");
-      s.textContent = it.s;
-      el.appendChild(d);
-      el.appendChild(b);
-      el.appendChild(s);
-      host.appendChild(el);
-    }
-  }
+  const steps = [
+    { b:"HTML5", s:"cadastro" },
+    { b:"SB", s:"atualizando" },
+    { b:"CAN", s:"coletando" },
+    { b:"Aprovar", s:"técnico" },
+    { b:"GS", s:"enviado" },
+    { b:"Fim", s:"ok" },
+  ];
+
+  const st = String(status || "").toUpperCase();
+  const host = $("stepper");
+  host.innerHTML = "";
+
+  const kinds = ["","","","","",""];
+  const pulse = [false,false,false,false,false,false];
+
+  // HTML5
+  if (st.includes("HTML5_ERROR")) { kinds[0] = "bad"; pulse[0] = true; }
+  else if (st.includes("HTML5_RUNNING") || st.includes("HTML5_QUEUED")) { kinds[0] = "warn"; pulse[0] = true; }
+  else if (st.includes("HTML5_DONE") || st.includes("SB") || st.includes("WAITING_REBOOT") || st.includes("CAN") || st.includes("GS") || st.includes("COMPLETED")) { kinds[0] = "good"; }
+
+  // SB
+  if (st.includes("SB_ERROR")) { kinds[1] = "bad"; pulse[1] = true; }
+  else if (st.includes("SB_RUNNING") || st.includes("SB_QUEUED")) { kinds[1] = "warn"; pulse[1] = true; }
+  else if (st.includes("SB_DONE") || st.includes("WAITING_REBOOT_CAN") || st.includes("CAN") || st.includes("GS") || st.includes("COMPLETED")) { kinds[1] = "good"; }
+
+  // CAN
+  if (st.includes("CAN_SNAPSHOT_ERROR")) { kinds[2] = "bad"; pulse[2] = true; }
+  else if (st.includes("CAN_RUNNING")) { kinds[2] = "good"; pulse[2] = true; }
+  else if (st.includes("CAN_SNAPSHOT_READY") || st.includes("CAN_APPROVED") || st.includes("GS") || st.includes("COMPLETED")) { kinds[2] = "good"; }
+  else if (st.includes("WAITING_REBOOT_CAN") || st.includes("SB_RUNNING")) { kinds[2] = ""; } // cinza
+
+  // Aprovar técnico
+  if (st.includes("CAN_APPROVED")) { kinds[3] = "good"; }
+  else if (st.includes("CAN_SNAPSHOT_READY")) { kinds[3] = "warn"; pulse[3] = true; }
+
+  // GS
+  if (st.includes("GS_ERROR")) { kinds[4] = "bad"; pulse[4] = true; }
+  else if (st.includes("GS_QUEUED") || st.includes("GS_RUNNING")) { kinds[4] = "warn"; pulse[4] = true; }
+  else if (st.includes("GS_DONE") || st.includes("COMPLETED")) { kinds[4] = "good"; }
+
+  // Fim
+  if (st.includes("COMPLETED")) { kinds[5] = "good"; }
+
+  for (let i=0;i<steps.length;i++){
+    const it = steps[i];
+    const el = document.createElement("div");
+    el.className = "step";
+    const d = document.createElement("span");
+    d.className = "dot " + (kinds[i] || "");
+    if (pulse[i]) d.classList.add("pulse");
+    const b = document.createElement("b");
+    b.textContent = it.b;
+    const s = document.createElement("span");
+    s.textContent = it.s;
+    el.appendChild(d);
+    el.appendChild(b);
+    el.appendChild(s);
+    host.appendChild(el);
+  }
+}
+
 
   function renderTop(inst){
     const top = $("topKv");
@@ -906,19 +1100,25 @@
 const id = inst?.installation_id || inst?.id || "";
 const tok = inst?.installation_token || inst?.token || "";
 
-// VALIDAR CAN: somente quando estivermos no estágio CAN+ (ou depois)
-const canApproveEnabled = !!(id && tok) && (st.includes("CAN") || st.includes("WAITING_REBOOT") || st.includes("GS") || st.includes("COMPLETED"));
+// buttons enabled/disabled after snapshot (v2)
+
+    // snapshot
+    const snap = pickStableSnapshot(inst);
+    state.lastSnapshot = snap;
+
+// enable buttons (v2): evita validar durante ciclos/WAITING_REBOOT e reduz corrida com finalize do worker
+const alreadyApproved = st.includes("CAN_APPROVED");
+const canApproveEnabled =
+  !!(id && tok) &&
+  !alreadyApproved &&
+  (st.includes("CAN_SNAPSHOT_READY") || (st.includes("CAN_RUNNING") && canReadyByData(snap)));
 $("btnApprove").disabled = !canApproveEnabled;
 
-// Atualizar CAN: só quando CAN já iniciou em algum momento e não estiver coletando/aguardando reboot/SB
 const canRefreshEnabled =
   !!(id && tok) &&
-  (st.includes("CAN") || st.includes("WAITING_REBOOT")) &&
-  !(st.includes("CAN_RUNNING") || st.includes("WAITING_REBOOT") || st.includes("SB_RUNNING"));
+  (st.includes("CAN_SNAPSHOT_READY") || st.includes("CAN_SNAPSHOT_ERROR") || st.includes("GS_ERROR"));
 $("btnRefresh").disabled = !canRefreshEnabled;
-    // snapshot
-    const snap = extractSnapshot(inst);
-    state.lastSnapshot = snap;
+
 
     const sb = parseSbProgress(inst, snap);
     $("sbPct").textContent = (sb == null) ? "—" : (sb + "%");
@@ -937,12 +1137,19 @@
       $("detailsDebug").classList.add("hidden");
     }
 
-    // status line
-    if (!id) setSubline("Pronto");
-    else {
-      const human = statusToLabel(status);
-      setSubline(human);
-    }
+    // status line + indicador visual (rodando / ok / erro)
+if (!id) setSubline("Pronto", "", false);
+else {
+  const human = statusToLabel(status);
+  let k = "";
+  let p = false;
+  if (st.includes("ERROR") || st.includes("GS_ERROR") || st.includes("SB_ERROR") || st.includes("HTML5_ERROR")) { k = "bad"; p = true; }
+  else if (st.includes("CAN_RUNNING")) { k = "good"; p = true; }
+  else if (st.includes("SB_RUNNING") || st.includes("HTML5_RUNNING") || st.includes("HTML5_QUEUED") || st.includes("WAITING_REBOOT_CAN")) { k = "warn"; p = true; }
+  else if (st.includes("CAN_SNAPSHOT_READY") || st.includes("CAN_APPROVED") || st.includes("COMPLETED")) { k = "good"; p = false; }
+  setSubline(human, k, p);
+}
+
   }
 
   function renderEmpty(){
@@ -968,7 +1175,11 @@
   }
 
   async function loadInstallation(id){
-    if (!id) return;
+  if (!id) return;
+  state.isLoading = true;
+  // feedback visual mesmo sem ações manuais
+  setLive("warn", true);
+  try {
     const r = await api("/api/installations/" + encodeURIComponent(id));
     if (!r.ok){
       showToast("Falha ao carregar instalação (HTTP " + r.status + ")", "bad");
@@ -979,9 +1190,12 @@
     // stop auto poll on completed
     const st = String(r.data?.status || "").toUpperCase();
     if (st.includes("COMPLETED")) stopPoll();
-  }
-
-  function startPoll(){
+  } finally {
+    state.isLoading = false;
+  }
+}
+
+function startPoll(){
     stopPoll();
     state.pollTimer = setInterval(async () => {
       const id = state?.inst?.installation_id || state?.inst?.id || (loadLs().id) || "";

--- a/dist/public/app_installations_v1.html
+++ b/dist/public/app_installations_v1.html
@@ -19,7 +19,7 @@
       border-bottom:1px solid var(--line);padding:14px 16px;display:flex;align-items:center;justify-content:space-between;gap:10px}
     header .title{display:flex;flex-direction:column;gap:2px}
     header h1{margin:0;font-size:14px;letter-spacing:.2px}
-    header .sub{font-size:12px;color:var(--muted)}
+    header .sub{font-size:12px;color:var(--muted);display:flex;align-items:center;gap:8px}
 
     .wrap{max-width:980px;margin:0 auto;padding:14px 12px 96px}
 
@@ -98,13 +98,19 @@
       .wrap{padding:12px 10px 98px}
       .chip .msg{max-width:160px}
     }
-  </style>
+  
+
+@keyframes pulse{0%{transform:scale(1);opacity:.55}50%{transform:scale(1.6);opacity:1}100%{transform:scale(1);opacity:.55}}
+@keyframes spin{to{transform:rotate(360deg)}}
+.dot.pulse{animation:pulse 1.1s ease-in-out infinite}
+.spin{display:inline-block;animation:spin 1s linear infinite}
+</style>
 </head>
 <body>
 <header>
   <div class="title">
     <h1>APP V1 — Instalações <span class="pill" style="margin-left:8px">v1.5</span></h1>
-    <div class="sub" id="subline">Pronto</div>
+    <div class="sub" id="subline"><span class="dot" id="liveDot"></span><span id="subText">Pronto</span></div>
   </div>
   <div style="display:flex;gap:10px;align-items:center">
     <div class="kv" id="topKv"></div>
@@ -247,16 +253,32 @@
   const $ = (id) => document.getElementById(id);
 
   // ====== state ======
-  const state = {
-    inst: null,
-    lastSnapshot: null,
-    pollTimer: null,
-    pollMs: 5000,
-    debug: false,
-  };
+const state = {
+  inst: null,
+  lastSnapshot: null,
+  pollTimer: null,
+  pollMs: 5000,
+  debug: false,
+  isLoading: false,
+  bestSnapshot: null,
+  bestSnapshotScore: 0,
+  bestSnapshotAt: 0,
+};
 
   // ====== ui helpers ======
-  function setSubline(msg){ $("subline").textContent = msg || ""; }
+
+function setLive(kind, pulse){
+  const d = $("liveDot");
+  if (!d) return;
+  d.className = "dot" + (kind ? (" " + kind) : "");
+  if (pulse) d.classList.add("pulse");
+}
+function setSubline(msg, kind="", pulse=false){
+  const t = $("subText") || $("subline");
+  if (t) t.textContent = msg || "";
+  setLive(kind, pulse);
+}
+
 
   function pill(text, kind){
     const s = document.createElement("span");
@@ -561,21 +583,93 @@
   }
 
   function extractSnapshot(inst){
-    const raw =
-      inst?.can_snapshot_latest ||
-      inst?.canSnapshotLatest ||
-      inst?.can_snapshot_best ||
-      inst?.canSnapshotBest ||
-      inst?.can_snapshot ||
-      inst?.canSnapshot ||
-      inst?.can?.snapshot_latest ||
-      inst?.can?.snapshot ||
-      (Array.isArray(inst?.can?.snapshots) ? inst.can.snapshots[inst.can.snapshots.length-1] : null) ||
-      null;
-
-    const snap = unwrapSnapshot(raw);
-    return snap && typeof snap === "object" ? snap : null;
-  }
+  // IMPORTANTE:
+  // - can_snapshot = "melhor snapshot" (persistido apenas quando tem dados)
+  // - can_snapshot_latest = último ciclo (pode vir parcial e não deve apagar UI)
+  const raw =
+    inst?.can_snapshot ||
+    inst?.canSnapshot ||
+    inst?.can_snapshot_best ||
+    inst?.canSnapshotBest ||
+    inst?.can?.snapshot ||
+    inst?.can?.snapshot_best ||
+    inst?.can_snapshot_latest ||
+    inst?.canSnapshotLatest ||
+    inst?.can?.snapshot_latest ||
+    (Array.isArray(inst?.can?.snapshots) ? inst.can.snapshots[0] : null) ||
+    null;
+
+  const snap = unwrapSnapshot(raw);
+  return snap && typeof snap === "object" ? snap : null;
+}
+
+
+
+function snapshotCounts(snapshot){
+  const c = (snapshot && snapshot.counts && typeof snapshot.counts === "object") ? snapshot.counts : {};
+  const params = Array.isArray(snapshot?.parameters) ? snapshot.parameters : (Array.isArray(snapshot?.params) ? snapshot.params : []);
+  const ms = Array.isArray(snapshot?.moduleState) ? snapshot.moduleState :
+             (Array.isArray(snapshot?.module_state) ? snapshot.module_state :
+              (Array.isArray(snapshot?.moduleStates) ? snapshot.moduleStates : []));
+  const paramsTotal = Number(c.params_total ?? c.parameters_total ?? params.length ?? 0) || 0;
+  const paramsWith = Number(c.params_with_value ?? c.parameters_with_value ?? 0) || 0;
+  const msKey = Number(c.module_state_key ?? c.moduleStateKey ?? 0) || 0;
+  const msTotal = Number(c.module_state_total ?? c.moduleStateTotal ?? ms.length ?? 0) || 0;
+  return { paramsTotal, paramsWith, msKey, msTotal };
+}
+
+function snapshotScore(snapshot){
+  if (!snapshot || typeof snapshot !== "object") return -1;
+  const { paramsTotal, paramsWith, msKey, msTotal } = snapshotCounts(snapshot);
+  const header = snapshot?.header || {};
+  const hasHdr = !!(header && (header.communication || header.server_time || header.license_nmbr || header.license_number || header.inner_id || header.serial || header.client || header.model || header.manufacturer));
+  // score forte para manter snapshot "bom" (evita flicker quando vier parcial)
+  return (hasHdr ? 100000 : 0) + (msKey * 5000) + (paramsWith * 50) + (paramsTotal) + (msTotal * 10);
+}
+
+function pickStableSnapshot(inst){
+  const best = extractSnapshot(inst);
+  const latest = unwrapSnapshot(inst?.can_snapshot_latest || inst?.canSnapshotLatest || inst?.can?.snapshot_latest || null);
+  const candidates = [best, latest].filter(Boolean);
+
+  // mantém o melhor na tela (não apaga por snapshot parcial)
+  let chosen = candidates.length ? candidates[0] : null;
+  let chosenScore = chosen ? snapshotScore(chosen) : -1;
+
+  for (const s of candidates.slice(1)){
+    const sc = snapshotScore(s);
+    if (sc > chosenScore) { chosen = s; chosenScore = sc; }
+  }
+
+  // se vier um snapshot muito fraco, não derruba o último válido
+  if (state.bestSnapshot){
+    const prevScore = state.bestSnapshotScore || snapshotScore(state.bestSnapshot);
+    if (chosenScore < 1 && prevScore > 0) {
+      chosen = state.bestSnapshot;
+      chosenScore = prevScore;
+    } else if (chosen && chosenScore >= prevScore) {
+      state.bestSnapshot = chosen;
+      state.bestSnapshotScore = chosenScore;
+      state.bestSnapshotAt = Date.now();
+    }
+  } else if (chosen && chosenScore > 0) {
+    state.bestSnapshot = chosen;
+    state.bestSnapshotScore = chosenScore;
+    state.bestSnapshotAt = Date.now();
+  }
+
+  return chosen || state.bestSnapshot || null;
+}
+
+
+function canReadyByData(snapshot){
+  const { paramsWith, msKey } = snapshotCounts(snapshot || {});
+  // regra simples e auditável (ajuste fino depois):
+  // - >= 3 módulos chave presentes
+  // - >= 6 parâmetros com valor (raw_value/value)
+  return (msKey >= 3) && (paramsWith >= 6);
+}
+
 
   function parsePacketAge(snapshot){
     const header = snapshot?.header || snapshot?.meta || {};
@@ -639,49 +733,70 @@
     return /^(1|true|yes|on)$/i.test(String(v || "").trim());
   }
 
-  function renderModuleState(snapshot){
-    const ms = snapshot?.module_state || snapshot?.moduleState || snapshot?.module_states || snapshot?.moduleStates || [];
-    const arr = Array.isArray(ms) ? ms : [];
-
-    const byId = new Map();
-    for (const it of arr){
-      const id = normMsId(it);
-      if (id != null && !byId.has(id)) byId.set(id, it);
-    }
-
-    const host = $("msChips");
-    host.innerHTML = "";
-
-    for (const w of MS_WANT){
-      const it = byId.get(w.id) || null;
-      const active = truthy(it?.active);
-      const ok = truthy(it?.ok);
-      const wasOk = truthy(it?.was_ok ?? it?.wasOk);
-      const msg = norm(it?.message || it?.msg || "");
-
-      let kind = "bad";
-      if (it && active && ok && wasOk) kind = "good";
-      else if (!it) kind = "warn";
-
-      const chip = document.createElement("div");
-      chip.className = "chip";
-      const d = document.createElement("span");
-      d.className = "dot " + kind;
-      const n = document.createElement("span");
-      n.className = "name";
-      n.textContent = w.key;
-      const m = document.createElement("span");
-      m.className = "msg";
-      if (!it) m.textContent = "sem dados";
-      else if (msg) m.textContent = msg;
-      else m.textContent = (kind === "good") ? "OK" : "falha";
-
-      chip.appendChild(d);
-      chip.appendChild(n);
-      chip.appendChild(m);
-      host.appendChild(chip);
-    }
-  }
+  function normMsKey(s){
+  return String(s || "").toUpperCase().replace(/[^A-Z0-9]/g, "");
+}
+
+function msKeyFromRow(it){
+  const s = normMsKey(it?.key || it?.sub || it?.sub_module_name || it?.subModuleName || it?.module_name || it?.moduleName || it?.name);
+  if (!s) return null;
+  if (s.includes("CAN0")) return "CAN0";
+  if (s.includes("CAN1")) return "CAN1";
+  if (s.includes("J1708")) return "J1708";
+  if (s.includes("KEYPADDALLAS") || s.includes("DALLAS") || s.includes("IBUTTON")) return "KEYPAD_DALLAS";
+  if (s.includes("KEYPADRAMZOR") || s.includes("RAMZOR")) return "KEYPAD_RAMZOR";
+  return null;
+}
+
+function renderModuleState(snapshot){
+  const ms = snapshot?.module_state || snapshot?.moduleState || snapshot?.module_states || snapshot?.moduleStates || snapshot?.moduleState || [];
+  const arr = Array.isArray(ms) ? ms : [];
+
+  const byId = new Map();
+  const byKey = new Map();
+  for (const it of arr){
+    const id = normMsId(it);
+    if (id != null && !byId.has(id)) byId.set(id, it);
+
+    const k = msKeyFromRow(it);
+    if (k && !byKey.has(k)) byKey.set(k, it);
+  }
+
+  const host = $("msChips");
+  host.innerHTML = "";
+
+  for (const w of MS_WANT){
+    const it = byKey.get(w.key) || byId.get(w.id) || null;
+
+    const active = truthy(it?.active);
+    const ok = truthy(it?.ok);
+    const wasOk = truthy(it?.was_ok ?? it?.wasOk);
+    const msg = norm(it?.message || it?.msg || it?.error_descr || it?.errorDescr || "");
+
+    let kind = "bad";
+    if (it && active && ok && wasOk) kind = "good";
+    else if (!it) kind = "";
+
+    const chip = document.createElement("div");
+    chip.className = "chip";
+    const d = document.createElement("span");
+    d.className = "dot " + (kind || "");
+    const n = document.createElement("span");
+    n.className = "name";
+    n.textContent = w.key;
+    const m = document.createElement("span");
+    m.className = "msg";
+    if (!it) m.textContent = "—";
+    else if (msg) m.textContent = msg;
+    else m.textContent = (kind === "good") ? "OK" : "falha";
+
+    chip.appendChild(d);
+    chip.appendChild(n);
+    chip.appendChild(m);
+    host.appendChild(chip);
+  }
+}
+
 
   function hasValue(v){
     if (v === 0) return true;
@@ -691,10 +806,48 @@
     return s !== "" && s.toLowerCase() !== "null" && s.toLowerCase() !== "nan";
   }
 
-  function pickKeyParams(snapshot, maxN){
-    const params = snapshot?.parameters || snapshot?.params || [];
-    const arr = Array.isArray(params) ? params : [];
-
+  const KEY_PARAM_RULES = [
+  { key: "speed",       label: "Speed",           re: /(vehicle\s*speed|\bspeed\b|veloc)/i },
+  { key: "rpm",         label: "RPM",             re: /(engine\s*speed|\brpm\b)/i },
+  { key: "fuel_level",  label: "Fuel level",      re: /(fuel\s*level|fuel\s*percent|nivel\s*comb|combust)/i },
+  { key: "fuel_used",   label: "Fuel used",       re: /(fuel\s*used|consumption|consumo|liter|l\/100|km\/?l)/i },
+  { key: "coolant",     label: "Coolant temp",    re: /(coolant|engine\s*temp|temp.*agua|temper)/i },
+  { key: "oil_press",   label: "Oil pressure",    re: /(oil\s*press|press.*oleo|engine\s*oil)/i },
+  { key: "brake_press", label: "Brake pressure",  re: /(brake\s*press|air\s*press|freio\s*press)/i },
+  { key: "battery",     label: "Battery voltage", re: /(battery\s*voltage|batt|tensao|voltag)/i },
+  { key: "mileage",     label: "Mileage / Odo",   re: /(gps\s*odo|odo|odomet|mileage|total\s*distance|km)/i },
+  { key: "hours",       label: "Engine hours",    re: /(engine\s*hours|\bhours\b|horimetro|hora)/i },
+];
+
+function pickKeyParams(snapshot, maxN){
+  const params = snapshot?.parameters || snapshot?.params || [];
+  const arr = Array.isArray(params) ? params : [];
+
+  const picked = [];
+  const used = new Set();
+
+  // 1) Regras (fuel/press/temp/etc)
+  for (const rule of KEY_PARAM_RULES){
+    for (const p of arr){
+      const name = String(p?.name || p?.param_name || p?.paramName || "");
+      if (!name) continue;
+      if (!rule.re.test(name)) continue;
+
+      const val = hasValue(p?.value) ? p.value : (hasValue(p?.raw_value) ? p.raw_value : null);
+      if (!hasValue(val)) continue;
+
+      const k = String(rule.key) + "|" + name;
+      if (used.has(k)) continue;
+      used.add(k);
+
+      picked.push({ name, val });
+      break;
+    }
+    if (picked.length >= maxN) break;
+  }
+
+  // 2) Complemento: ranking geral (quando faltou algo)
+  if (picked.length < maxN){
     const scored = [];
     for (const p of arr){
       const name = String(p?.name || p?.param_name || p?.paramName || "");
@@ -704,40 +857,29 @@
       const n = name.toLowerCase();
       let score = 0;
       if (/(speed|veloc)/.test(n)) score += 50;
-      if (/(rpm)/.test(n)) score += 45;
+      if (/(rpm|engine\s*speed)/.test(n)) score += 45;
       if (/(mile|odo|odomet|km)/.test(n)) score += 35;
       if (/(fuel|comb|diesel|gas)/.test(n)) score += 30;
-      if (/(hour|hora)/.test(n)) score += 25;
-      if (/(temp|temper)/.test(n)) score += 20;
-      if (/(ignit|key)/.test(n)) score += 15;
-      scored.push({ p, name, val, score });
+      if (/(press)/.test(n)) score += 28;
+      if (/(temp|temper|coolant)/.test(n)) score += 25;
+      if (/(hour|hora)/.test(n)) score += 20;
+      scored.push({ name, val, score });
     }
 
     scored.sort((a,b)=> (b.score - a.score) || String(a.name).localeCompare(String(b.name)));
 
-    const out = [];
-    const seen = new Set();
     for (const it of scored){
-      const k = it.name || String(it.p?.id || "");
-      if (!k || seen.has(k)) continue;
-      seen.add(k);
-      out.push({ name: it.name || ("id " + String(it.p?.id || "?")), val: it.val });
-      if (out.length >= maxN) break;
-    }
-
-    // fallback: pega os primeiros com valor (se nada ranqueado)
-    if (!out.length){
-      for (const p of arr){
-        const name = String(p?.name || p?.param_name || p?.paramName || "");
-        const val = hasValue(p?.value) ? p.value : (hasValue(p?.raw_value) ? p.raw_value : null);
-        if (!hasValue(val)) continue;
-        out.push({ name: name || ("id " + String(p?.id || "?")), val });
-        if (out.length >= maxN) break;
-      }
-    }
-
-    return out;
-  }
+      const k = it.name;
+      if (!k || used.has(k)) continue;
+      used.add(k);
+      picked.push({ name: it.name, val: it.val });
+      if (picked.length >= maxN) break;
+    }
+  }
+
+  return picked.slice(0, maxN);
+}
+
 
   function renderKeyParams(snapshot){
     const host = $("keyParams");
@@ -757,6 +899,25 @@
     top.className = "li";
     top.innerHTML = `<div class="k">Parameters</div><div class="v">${withVal}/${total}</div>`;
     host.appendChild(top);
+
+
+// Header "principais" (quando disponível)
+const hdr = (snapshot && snapshot.header && typeof snapshot.header === "object") ? snapshot.header : {};
+const hdrRows = [
+  ["Ignition", hdr.ignition ?? (hdr.raw && hdr.raw.ignition)],
+  ["Speed", hdr.speed],
+  ["Fuel", hdr.fuel],
+  ["Mileage", hdr.mileage],
+  ["Engine hours", hdr.engine_hours],
+];
+
+for (const [kName, vVal] of hdrRows){
+  if (!hasValue(vVal)) continue;
+  const li = document.createElement("div");
+  li.className = "li";
+  li.innerHTML = `<div class="k">${kName}</div><div class="v">${String(vVal)}</div>`;
+  host.appendChild(li);
+}
 
     const picked = pickKeyParams(snapshot, 8);
     for (const it of picked){
@@ -800,35 +961,68 @@
   }
 
   function renderStepper(status){
-    const steps = [
-      { b:"HTML5", s:"cadastro" },
-      { b:"SB", s:"atualizando" },
-      { b:"CAN", s:"coletando" },
-      { b:"Aprovar", s:"técnico" },
-      { b:"GS", s:"enviado" },
-      { b:"Fim", s:"ok" },
-    ];
-
-    const idx = statusToStepIndex(status);
-    const host = $("stepper");
-    host.innerHTML = "";
-
-    for (let i=0;i<steps.length;i++){
-      const it = steps[i];
-      const el = document.createElement("div");
-      el.className = "step";
-      const d = document.createElement("span");
-      d.className = "dot " + (i < idx ? "good" : (i === idx ? "warn" : ""));
-      const b = document.createElement("b");
-      b.textContent = it.b;
-      const s = document.createElement("span");
-      s.textContent = it.s;
-      el.appendChild(d);
-      el.appendChild(b);
-      el.appendChild(s);
-      host.appendChild(el);
-    }
-  }
+  const steps = [
+    { b:"HTML5", s:"cadastro" },
+    { b:"SB", s:"atualizando" },
+    { b:"CAN", s:"coletando" },
+    { b:"Aprovar", s:"técnico" },
+    { b:"GS", s:"enviado" },
+    { b:"Fim", s:"ok" },
+  ];
+
+  const st = String(status || "").toUpperCase();
+  const host = $("stepper");
+  host.innerHTML = "";
+
+  const kinds = ["","","","","",""];
+  const pulse = [false,false,false,false,false,false];
+
+  // HTML5
+  if (st.includes("HTML5_ERROR")) { kinds[0] = "bad"; pulse[0] = true; }
+  else if (st.includes("HTML5_RUNNING") || st.includes("HTML5_QUEUED")) { kinds[0] = "warn"; pulse[0] = true; }
+  else if (st.includes("HTML5_DONE") || st.includes("SB") || st.includes("WAITING_REBOOT") || st.includes("CAN") || st.includes("GS") || st.includes("COMPLETED")) { kinds[0] = "good"; }
+
+  // SB
+  if (st.includes("SB_ERROR")) { kinds[1] = "bad"; pulse[1] = true; }
+  else if (st.includes("SB_RUNNING") || st.includes("SB_QUEUED")) { kinds[1] = "warn"; pulse[1] = true; }
+  else if (st.includes("SB_DONE") || st.includes("WAITING_REBOOT_CAN") || st.includes("CAN") || st.includes("GS") || st.includes("COMPLETED")) { kinds[1] = "good"; }
+
+  // CAN
+  if (st.includes("CAN_SNAPSHOT_ERROR")) { kinds[2] = "bad"; pulse[2] = true; }
+  else if (st.includes("CAN_RUNNING")) { kinds[2] = "good"; pulse[2] = true; }
+  else if (st.includes("CAN_SNAPSHOT_READY") || st.includes("CAN_APPROVED") || st.includes("GS") || st.includes("COMPLETED")) { kinds[2] = "good"; }
+  else if (st.includes("WAITING_REBOOT_CAN") || st.includes("SB_RUNNING")) { kinds[2] = ""; } // cinza
+
+  // Aprovar técnico
+  if (st.includes("CAN_APPROVED")) { kinds[3] = "good"; }
+  else if (st.includes("CAN_SNAPSHOT_READY")) { kinds[3] = "warn"; pulse[3] = true; }
+
+  // GS
+  if (st.includes("GS_ERROR")) { kinds[4] = "bad"; pulse[4] = true; }
+  else if (st.includes("GS_QUEUED") || st.includes("GS_RUNNING")) { kinds[4] = "warn"; pulse[4] = true; }
+  else if (st.includes("GS_DONE") || st.includes("COMPLETED")) { kinds[4] = "good"; }
+
+  // Fim
+  if (st.includes("COMPLETED")) { kinds[5] = "good"; }
+
+  for (let i=0;i<steps.length;i++){
+    const it = steps[i];
+    const el = document.createElement("div");
+    el.className = "step";
+    const d = document.createElement("span");
+    d.className = "dot " + (kinds[i] || "");
+    if (pulse[i]) d.classList.add("pulse");
+    const b = document.createElement("b");
+    b.textContent = it.b;
+    const s = document.createElement("span");
+    s.textContent = it.s;
+    el.appendChild(d);
+    el.appendChild(b);
+    el.appendChild(s);
+    host.appendChild(el);
+  }
+}
+
 
   function renderTop(inst){
     const top = $("topKv");
@@ -906,19 +1100,25 @@
 const id = inst?.installation_id || inst?.id || "";
 const tok = inst?.installation_token || inst?.token || "";
 
-// VALIDAR CAN: somente quando estivermos no estágio CAN+ (ou depois)
-const canApproveEnabled = !!(id && tok) && (st.includes("CAN") || st.includes("WAITING_REBOOT") || st.includes("GS") || st.includes("COMPLETED"));
+// buttons enabled/disabled after snapshot (v2)
+
+    // snapshot
+    const snap = pickStableSnapshot(inst);
+    state.lastSnapshot = snap;
+
+// enable buttons (v2): evita validar durante ciclos/WAITING_REBOOT e reduz corrida com finalize do worker
+const alreadyApproved = st.includes("CAN_APPROVED");
+const canApproveEnabled =
+  !!(id && tok) &&
+  !alreadyApproved &&
+  (st.includes("CAN_SNAPSHOT_READY") || (st.includes("CAN_RUNNING") && canReadyByData(snap)));
 $("btnApprove").disabled = !canApproveEnabled;
 
-// Atualizar CAN: só quando CAN já iniciou em algum momento e não estiver coletando/aguardando reboot/SB
 const canRefreshEnabled =
   !!(id && tok) &&
-  (st.includes("CAN") || st.includes("WAITING_REBOOT")) &&
-  !(st.includes("CAN_RUNNING") || st.includes("WAITING_REBOOT") || st.includes("SB_RUNNING"));
+  (st.includes("CAN_SNAPSHOT_READY") || st.includes("CAN_SNAPSHOT_ERROR") || st.includes("GS_ERROR"));
 $("btnRefresh").disabled = !canRefreshEnabled;
-    // snapshot
-    const snap = extractSnapshot(inst);
-    state.lastSnapshot = snap;
+
 
     const sb = parseSbProgress(inst, snap);
     $("sbPct").textContent = (sb == null) ? "—" : (sb + "%");
@@ -937,12 +1137,19 @@
       $("detailsDebug").classList.add("hidden");
     }
 
-    // status line
-    if (!id) setSubline("Pronto");
-    else {
-      const human = statusToLabel(status);
-      setSubline(human);
-    }
+    // status line + indicador visual (rodando / ok / erro)
+if (!id) setSubline("Pronto", "", false);
+else {
+  const human = statusToLabel(status);
+  let k = "";
+  let p = false;
+  if (st.includes("ERROR") || st.includes("GS_ERROR") || st.includes("SB_ERROR") || st.includes("HTML5_ERROR")) { k = "bad"; p = true; }
+  else if (st.includes("CAN_RUNNING")) { k = "good"; p = true; }
+  else if (st.includes("SB_RUNNING") || st.includes("HTML5_RUNNING") || st.includes("HTML5_QUEUED") || st.includes("WAITING_REBOOT_CAN")) { k = "warn"; p = true; }
+  else if (st.includes("CAN_SNAPSHOT_READY") || st.includes("CAN_APPROVED") || st.includes("COMPLETED")) { k = "good"; p = false; }
+  setSubline(human, k, p);
+}
+
   }
 
   function renderEmpty(){
@@ -968,7 +1175,11 @@
   }
 
   async function loadInstallation(id){
-    if (!id) return;
+  if (!id) return;
+  state.isLoading = true;
+  // feedback visual mesmo sem ações manuais
+  setLive("warn", true);
+  try {
     const r = await api("/api/installations/" + encodeURIComponent(id));
     if (!r.ok){
       showToast("Falha ao carregar instalação (HTTP " + r.status + ")", "bad");
@@ -979,9 +1190,12 @@
     // stop auto poll on completed
     const st = String(r.data?.status || "").toUpperCase();
     if (st.includes("COMPLETED")) stopPoll();
-  }
-
-  function startPoll(){
+  } finally {
+    state.isLoading = false;
+  }
+}
+
+function startPoll(){
     stopPoll();
     state.pollTimer = setInterval(async () => {
       const id = state?.inst?.installation_id || state?.inst?.id || (loadLs().id) || "";

--- a/src/routes/jobRoutes.ts
+++ b/src/routes/jobRoutes.ts
@@ -225,9 +225,27 @@
         last_snapshot_at: (__snap && (__snap.captured_at || __snap.capturedAt)) || ((can && (can.last_snapshot_at || can.lastSnapshotAt)) ? (can.last_snapshot_at || can.lastSnapshotAt) : null),
       });
 
-      installationsStore.patchInstallation(installationId, { can: __canPatched,
-    can_snapshot_latest: (__snap || null),
-    can_snapshot: (__hasData ? __snap : null), status: (__hasData ? "CAN_SNAPSHOT_READY" : "CAN_SNAPSHOT_ERROR") });
+      
+const curStatus = String((inst as any)?.status || "").toUpperCase();
+const keepStatus =
+  curStatus.includes("CAN_APPROVED") ||
+  curStatus.startsWith("GS_") ||
+  curStatus.includes("COMPLETED") ||
+  (curStatus.includes("ERROR") && !curStatus.startsWith("CAN_"));
+
+const prevBest = (inst as any).can_snapshot || (inst as any).canSnapshot || null;
+const nextStatus = (__hasData ? "CAN_SNAPSHOT_READY" : "CAN_SNAPSHOT_ERROR");
+
+const patch: any = {
+  can: __canPatched,
+  can_snapshot_latest: (__snap || null),
+  // não apagar o último snapshot "bom" quando vier um ciclo vazio
+  can_snapshot: (__hasData ? __snap : (prevBest || null)),
+};
+
+if (!keepStatus) patch.status = nextStatus;
+
+installationsStore.patchInstallation(installationId, patch);
     } catch {}
 
     try {

--- a/src/routes/installationsRoutes.ts
+++ b/src/routes/installationsRoutes.ts
@@ -579,8 +579,10 @@
 
     const topPatch: any = {
       can: canPatched,
-      can_snapshot_latest: snap,
-      can_snapshot: hasData ? snap : null,
+
+can_snapshot_latest: hasData ? snap : ((inst as any).can_snapshot_latest || (inst as any).canSnapshotLatest || snap),
+// mantém o "melhor snapshot" (não zera quando vier parcial)
+can_snapshot: hasData ? snap : ((inst as any).can_snapshot || (inst as any).canSnapshot || null),
     };
 
     // opcional: status vindo do worker

--- a/dist/worker/canSnapshotWorker.js
+++ b/dist/worker/canSnapshotWorker.js
@@ -607,27 +607,68 @@
 function __cs_summarizeSnapshot(snap){
   if (!snap || typeof snap !== "object") return null;
   const params = Array.isArray(snap.parameters) ? snap.parameters : [];
-  const ms = Array.isArray(snap.moduleState) ? snap.moduleState : [];
+    const ms =
+    Array.isArray(snap.moduleState) ? snap.moduleState :
+    (Array.isArray(snap.module_state) ? snap.module_state : []);
   const idsKeep = new Set(["8","9","15","19","20"]);
 
-  const pickedMs = ms.filter(function(r){
-    const id = String((r && (r.id || r.module_id || r.moduleId)) || "").trim();
-    return idsKeep.has(id);
-  }).map(function(r){
-    return {
+function __toBool(v){
+  if (typeof v === "boolean") return v;
+  if (typeof v === "number") return v !== 0;
+  const s = String(v == null ? "" : v).trim().toLowerCase();
+  if (s === "1" || s === "true" || s === "yes" || s === "on") return true;
+  if (s === "0" || s === "false" || s === "no" || s === "off" || s === "") return false;
+  return !!v;
+}
+
+  function __normKey(s){
+    return String(s || "").toUpperCase().replace(/[^A-Z0-9]/g, "");
+  }
+  function __keyFromRow(r){
+    const s = __normKey((r && (r.key || r.sub || r.sub_module_name || r.subModuleName || r.module_name || r.moduleName || r.name)) || "");
+    if (!s) return null;
+    if (s.includes("CAN0")) return "CAN0";
+    if (s.includes("CAN1")) return "CAN1";
+    if (s.includes("J1708")) return "J1708";
+    if (s.includes("KEYPADDALLAS") || s.includes("DALLAS") || s.includes("IBUTTON")) return "KEYPAD_DALLAS";
+    if (s.includes("KEYPADRAMZOR") || s.includes("RAMZOR")) return "KEYPAD_RAMZOR";
+    return null;
+  }
+  function __keyFromId(idStr){
+    if (idStr === "8") return "CAN0";
+    if (idStr === "9") return "CAN1";
+    if (idStr === "15") return "J1708";
+    if (idStr === "19") return "KEYPAD_DALLAS";
+    if (idStr === "20") return "KEYPAD_RAMZOR";
+    return null;
+  }
+
+  const pickedMs = [];
+  const seen = new Set();
+  for (var mi=0; mi<ms.length; mi++){
+    var r = ms[mi] || {};
+    var id = String((r && (r.id || r.module_id || r.moduleId)) || "").trim();
+    var key = __keyFromRow(r) || (idsKeep.has(id) ? __keyFromId(id) : null);
+    if (!key) continue;
+    if (seen.has(key)) continue;
+    seen.add(key);
+
+    pickedMs.push({
+      key: key,
       id: (r && (r.id || r.module_id || r.moduleId) != null) ? String(r.id || r.module_id || r.moduleId) : null,
       name: (r && (r.name || r.module_name || r.moduleName)) ? String(r.name || r.module_name || r.moduleName) : null,
       module_name: (r && (r.module_name || r.moduleName || r.name)) ? String(r.module_name || r.moduleName || r.name) : null,
       module: (r && r.module) ? String(r.module) : null,
       sub: (r && (r.sub || r.sub_module_name || r.subModuleName)) ? String(r.sub || r.sub_module_name || r.subModuleName) : null,
-      active: !!(r && r.active),
-      ok: !!(r && r.ok),
-      was_ok: !!(r && r.was_ok),
-      error: !!(r && r.error),
+      active: __toBool(r && r.active),
+      ok: __toBool(r && r.ok),
+      was_ok: __toBool(r && (r.was_ok != null ? r.was_ok : (r.wasOk != null ? r.wasOk : null))),
+      error: __toBool(r && r.error),
+      message: (r && (r.message || r.msg || r.error_descr || r.errorDescr) != null) ? String(r.message || r.msg || r.error_descr || r.errorDescr) : null,
       error_descr: (r && r.error_descr != null) ? String(r.error_descr) : null,
       last_update_date: (r && (r.last_update_date || r.last_update || r.lastUpdate) != null) ? String(r.last_update_date || r.last_update || r.lastUpdate) : null,
-    };
-  });
+    });
+  }
 
   var hdr = (snap.header && typeof snap.header === "object") ? snap.header : null;
   var hdrLite = hdr ? {
@@ -652,6 +693,7 @@
     model: hdr.model || hdr.vcl_model || null,
     vcl_client_description: hdr.vcl_client_description || hdr.client || null,
     client: hdr.client || hdr.vcl_client_description || null,
+    ignition: (hdr.ignition != null ? hdr.ignition : null),
     speed: hdr.speed != null ? hdr.speed : null,
     fuel: hdr.fuel != null ? hdr.fuel : null,
     mileage: hdr.mileage != null ? hdr.mileage : null,

--- a/dist/services/installationsEngine.js
+++ b/dist/services/installationsEngine.js
@@ -346,36 +346,47 @@
     }
   } catch (_e) {}
 
+  
   // enfileira GS
   const gs = catalogs.resolveGsCommand({
     label_pos: inst.payload?.gsensor?.label_pos,
     harness_pos: inst.payload?.gsensor?.harness_pos
   });
-  if (!gs) {
-    store.patchInstallation(installationId, { status: "ERROR", error: { code: "GS_COMMAND_NOT_CONFIGURED" } });
+
+  const gsCmd = String(gs?.GS_COMMAND_SYNTAX || gs?.command || gs?.command_syntax || "").trim();
+  const gsActionId = Number(gs?.GS_ACTION_ID || gs?.action_id || process.env.GS_ACTION_ID_DEFAULT || 5);
+
+  if (!gsCmd) {
+    // mantém CAN aprovado, mas marca erro de GS (config)
+    store.patchInstallation(installationId, {
+      status: "GS_ERROR",
+      last_error: { ts: now, code: "GS_COMMAND_NOT_CONFIGURED", message: "gsensorMap sem comando (label/harness)" }
+    });
     return store.getInstallation(installationId);
   }
 
   const vid = (inst.resolved || {}).vehicle_id;
   const target_client_id = (inst.resolved && inst.resolved.target_client_id) || Number(inst.payload.target_client_id || 0) || null;
+  const current_client_id = (inst.resolved || {}).current_client_id || null;
+
   const vehicleSettingId = catalogs.resolveVehicleSettingId({
     target_client_id,
-    client_id: (inst.resolved || {}).current_client_id,
+    client_id: current_client_id,
     vehicleSettingId: inst.payload.vehicleSettingId,
     vehicle: inst.payload.vehicle
   });
 
-  const clientName = _clientName(target_client_id);
+  const clientName = _clientName(target_client_id || current_client_id);
   const gsJob = await enqueueJob({
     type: "monitor_gs",
     payload: {
       installation_id: installationId,
-      clientId: target_client_id,
+      clientId: target_client_id || current_client_id,
       clientName,
       vehicleId: vid,
       vehicleSettingId,
-      GS_ACTION_ID: gs.GS_ACTION_ID,
-      GS_COMMAND_SYNTAX: gs.GS_COMMAND_SYNTAX,
+      GS_ACTION_ID: gsActionId,
+      GS_COMMAND_SYNTAX: gsCmd,
       comment: inst.payload.comment || ("APP GS " + service),
       canApproved: true,
       override: overrideBool,
@@ -384,6 +395,7 @@
   });
 
   store.pushJob(installationId, { type: "monitor_gs", job_id: gsJob.id || gsJob.job_id || null, status: "queued" });
+  store.patchInstallation(installationId, { status: "GS_QUEUED" });
   return store.getInstallation(installationId);
 }
 
